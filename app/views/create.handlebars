<div class="admin-form">
  <div class="row">
    <div class="large-12 columns">
      <a href="/admin/articles" class="button tiny"><span class="fa fa-undo"></span> Volver</a>
    </div>
  </div>
  <form action="/articles" id="articles-form" method="post" enctype='multipart/form-data'>
  <div class="row">
    <div class="large-8 columns">
      <h1>{{title}}</h1>
      <input type="text" name="text" placeholder="texto" />
      <textarea name="fulltext" id="fulltext"></textarea>
    </div>
    <div class="large-4 columns">
      <button class="button success full">Crear articulo</button>
      <input type="text" name="title" placeholder="título"  />
      <select name="category" id="category">
        <option value="General">General</option>
        <option value="tips">Consejos medicos</option>
      </select>
      <input type="hidden" name="user" value="{{user.id}}" />
      <label for="image_upload">Imagen destacada</label>
      <div class="file">
        <input type="file" class="inline-file" name="image_upload" id="image_upload" />
      </div>
    </div>
  </div>
  </form>
</div>